package com.example.projetmpisi.cucumber.runner;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class CucumberIT {
    // Ce runner est volontairement vide.
    // Toutes les options sont configurées dans les annotations ou via application.properties.
}
